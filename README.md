
  # Health Tracking Audio Logger

  This is a code bundle for Health Tracking Audio Logger. The original project is available at https://www.figma.com/design/b2R4IdcYGnuxv84CKYzsp5/Health-Tracking-Audio-Logger.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  